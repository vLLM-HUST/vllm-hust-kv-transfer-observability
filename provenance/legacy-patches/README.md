# Legacy patch archive

This directory preserves original commits from core PRs #217, #220, #221, and
#236 as unmodified `git format-patch` exports. Author metadata, messages,
trailers, and exact diffs are retained.

Event and observer modules should become extension-owned; scheduler and model
runner insertion points require versioned host contracts.
